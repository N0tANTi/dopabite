# 2026-09-23 project status checkpoint

- Performed a documentation-only, read-only checkpoint; no product code, production configuration, database data, or deployment was changed.
- Confirmed `https://food.archein.site/` redirects HTTP to HTTPS and returns 200 over HTTPS. `/api/health` returns `{"status":"ok"}` and `/api/config` reports email OTP enabled.
- Confirmed web and API symlinks both point to release `20260922171200`, serving `index-q2yKtdik.js` and `index-Daop-U0j.css`.
- Confirmed the public community-ranking endpoint currently returns 43 restaurant snapshots, up from 24 at the release checkpoint as users added ratings.
- Confirmed Nginx, `dopabite-api.service`, and `dopabite-backup.timer` are active, and the Nginx configuration test passes.
- Verified the newest server-local snapshot, `dopabite-20260922T193607Z.sqlite3`, against SHA-256 `a925abf6b0dc3d12c7ce600e8b12054171de63a5bc0780220f76264a753f13c2`. The latest off-host copy remains `dopabite-20260922T091556Z.sqlite3` under `D:\anti\backups\dopabite`.
- Confirmed the Tencent Cloud host remains on its single 40 GB ext4 root filesystem with 26% byte use and 9% inode use.
- Confirmed `main` was clean and synchronized with `origin/main` before this record update.
