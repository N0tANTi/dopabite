# 2026-09-23 project status checkpoint

- Performed a documentation-only, read-only checkpoint; no product code, production configuration, database data, or deployment was changed.
- Confirmed `https://food.archein.site/` redirects HTTP to HTTPS and returns 200 over HTTPS. `/api/health` returns `{"status":"ok"}` and `/api/config` reports email OTP enabled.
- Confirmed web and API symlinks both point to release `20260922171200`, serving `index-q2yKtdik.js` and `index-Daop-U0j.css`.
- Confirmed the public community-ranking endpoint currently returns 43 restaurant snapshots, up from 24 at the release checkpoint as users added ratings.
- Confirmed Nginx, `dopabite-api.service`, and `dopabite-backup.timer` are active, and the Nginx configuration test passes.
- Verified the newest server-local snapshot, `dopabite-20260922T193607Z.sqlite3`, against SHA-256 `a925abf6b0dc3d12c7ce600e8b12054171de63a5bc0780220f76264a753f13c2`. The latest off-host copy remains `dopabite-20260922T091556Z.sqlite3` under `D:\anti\backups\dopabite`.
- Confirmed the Tencent Cloud host remains on its single 40 GB ext4 root filesystem with 26% byte use and 9% inode use.
- Confirmed `main` was clean and synchronized with `origin/main` before this record update.

## Web release 20260923022901

- Deployed Git `4afd4b5` to `/srv/dopabite/releases/20260923022901` after local `npm ci`, lint, and build. This adds all-location search from the empty results state, restores the three views on narrow screens, and lets a mobile swipe scroll over the map until the user enables map interaction.
- Preflight confirmed `/srv` on `/dev/vda2`, 29 GB available, 9% inode use, and active Nginx, API, and backup timer. Uploaded only the 2.9 MB static build; verified the release archive SHA-256 before extraction and promoted the symlink atomically. Kept releases `20260922161714` and `20260922171200` as rollback copies.
- Verified HTTP redirects to HTTPS; HTTPS, the built JS/CSS, `/api/health`, `/api/config`, and `/api/rankings` returned successfully. A fresh public browser loaded 40 live AMap POIs. Nginx syntax, certificate SAN, and service status passed; API release remained `20260922171200`. Disk use after release is 26% of bytes and 9% of inodes.
- Ran the backup service before deployment. Snapshot `dopabite-20260923T022803Z.sqlite3` was copied to `D:\anti\backups\dopabite`, matched SHA-256 `ff98ca3ecaaf06f50b3f3ed1aae1bb1b6eabe8eeb7468945f9f0d53a2fab4d8b`, and returned `ok` from SQLite integrity check.
- Physical-phone scrolling, map touch mode, and view switching remain for the user to test on the live site.

## Review photo release 20260923084340

- Deployed Git `18dc11c` to matching web and API releases. Reviews can now include up to three public images, with preview, edit/removal, public display, and deletion tied to the rating. Images live in the persistent SQLite database and are included in its backup. Nginx's request limit increased from 64 KB to 8 MB.
- Local `npm ci`, lint, build, and an isolated image lifecycle API integration test passed. The new API started successfully against a copy of the production snapshot before activation.
- Before migration, snapshot `dopabite-20260923T084304Z.sqlite3` passed its SHA-256 and SQLite integrity checks. The off-host copy matched SHA-256 `fe2d205f30563fa0b3c43dc8e94a917cf28872023459be190a29dc094157c9a2`.
- Production HTTPS, release asset identity, API health/config/ratings/rankings, missing-image response, a 100 KB unauthenticated upload reaching API authorization, Nginx syntax, and service status passed. The active release and two verified rollback releases remain for each service. Storage is 26% of bytes and 9% of inodes.
- A real photo was not posted to production during automated verification. Physical-phone camera/album selection, upload, edit, and display remain for the user to test.
