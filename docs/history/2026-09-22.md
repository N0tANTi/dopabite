# 2026-09-22 public repository and backend decision

- Created the public GitHub repository `https://github.com/N0tANTi/dopabite` and linked the production homepage.
- Replaced the README with a concise Chinese overview, live URL, feature list, setup instructions, data boundaries, and acknowledgements for `poi-marker` and Tastemap.
- Published a clean root history to keep the deleted local QA document and its machine-specific paths out of the public repository. The original local commits are retained in `archive/pre-public-history-20260922` and are intentionally not pushed.
- Verified that `.env.local`, credentials, dependencies, build output, and unused local assets are excluded from the public snapshot.
- Chose a progressive identity design: anonymous session for the first rating, Passkey for low-friction binding, future WeChat QR login, and email OTP only as a fallback.
- Chose a same-origin Node.js API with Better Auth and SQLite WAL for the first shared-rating backend. The decision reflects the current 2-core, 2 GB Tencent host and keeps persistent data outside release directories.
- Recorded the data model, API boundary, anti-abuse rules, privacy controls, backup requirements, local-rating import, and PostgreSQL migration triggers in `docs/architecture/shared-ratings.md`.

## Shared accounts and ratings deployment

- Implemented a same-origin Hono API with Better Auth anonymous sessions, Passkey enrollment/login, SQLite WAL migrations, public rating reads, per-user rating upserts, local-data import, and private saved-location sync.
- Replaced the inactive profile control with an account panel that explains data visibility, explicitly requests consent before uploading local favorites, supports Passkey binding, and clears the local personal cache on sign-out while keeping cloud data.
- Preserved offline behavior: rating writes remain in browser storage when the API is unavailable and can be merged later.
- Added input validation, same-origin write checks, bounded write limiting, secure response headers, non-public database storage, systemd hardening, API health checks, and atomic web/API release links.
- Fixed a React/Amap marker-root teardown race found during browser QA.
- Deployed web and API release `20260922110335` to `https://food.archein.site/`; enabled `dopabite-api.service` and daily verified backups.
- Verified two-user shared-rating behavior, private favorite isolation, rating update semantics, local-import preservation, HTTPS/API health, cross-origin rejection, certificate SAN, Nginx configuration, SQLite integrity, and post-deploy storage headroom.
- Copied the first production snapshot off-host with a matching checksum. Automated Tencent COS replication and a restore drill remain open.
- Restricted the persistent data directory, database, WAL files, and snapshots to the API service account; systemd now applies `UMask=0077` to future database and backup files.

## Nearby chain discovery correction

- Confirmed with live AMap results that nearby McDonald's, KFC, and Pizza Hut POIs existed but were omitted because the keyword “餐厅” applied relevance ranking before the first-page limit.
- Replaced that keyword-biased lookup with a 50-item dining-type pool, plus small sequential lookups that reserve at most one nearby result for each of the three familiar chains. The final list is deduplicated, distance-sorted, and still respects the user's requested count.
- Added stale-request protection so a slow earlier lookup cannot replace results after the user changes location or count.
- Verified the Hangzhou Wangjiang test point locally with all three chains present in a 40-item list and no browser console errors.
- Deployed web release `20260922113221`; HTTPS, API health, certificate SAN, Nginx configuration, served asset identity, and storage headroom all passed verification.

## Public profiles and passwordless email accounts

- Reworked the account hierarchy so email-code login is the primary cross-device path, Passkey is an optional method for supported devices, and WeChat login is explicitly deferred.
- Added generated and editable public nicknames. Public rating cards now show nickname, date, all three score dimensions, note, and an ownership marker without exposing email, authentication method, or internal user ID.
- Added generic SMTP delivery with runtime feature detection. If credentials are absent, `/api/config` disables the form and the account panel shows a clear pending state instead of offering a broken action.
- Added anonymous-to-email account linking that preserves ratings and private saved locations. If both accounts rated the same POI, the newer record wins; existing formal-account nicknames are preserved on later sign-ins.
- Verified the complete local flow with two sessions and deterministic test OTP: anonymous rating and favorite creation, public visibility from another session, email upgrade, merged ownership, and fresh email recovery all passed. Lint, production build, dependency audit, and desktop visual QA passed; the existing bundle-size warning remains tracked.
- Created and integrity-checked snapshot `dopabite-20260922T050158Z.sqlite3`, copied it off-host with a matching SHA-256, then deployed web and API release `20260922130417` atomically.
- Verified HTTPS and API health, disabled-mail runtime config, asset identity, cross-origin write rejection, Nginx configuration, service health, release retention, and 25% byte / 8% inode use after deployment.

## Tencent SES adapter staging

- Created a UTF-8, single-variable DopaBite HTML verification-code template at `deploy/email-templates/dopabite-login-code.html` and submitted its source to GitHub without credentials or external assets.
- Added server-only Tencent SES API delivery using TC3-HMAC-SHA256, template data `{"code":"..."}`, and transactional trigger mode while retaining SMTP as a fallback.
- Created and stored a least-privilege CAM credential only in `/etc/dopabite/api.env`, then configured sender `no-reply@notify.archein.site` and template ID `61514` without committing secrets.
- Corrected the operator connection record: DopaBite production uses local SSH alias `tengxunyun-anti`; the unrelated `aliyun-anti` host must never be used for this project.
- Snapshot `dopabite-20260922T062854Z.sqlite3` completed before deployment. API release `20260922142926` was promoted atomically and passed service, public health, environment permission, log, and capacity checks at 26% byte / 9% inode use. After the sender and template were added, `/api/config` changed to `{"emailOtpEnabled":true}` as intended.
- The first live send exposed an existing Better Auth option bug: production explicitly passed an undefined `generateOTP` callback instead of allowing the default random generator. The conditional option was corrected, release `20260922143538` was deployed, and a second live send returned `{"success":true}`. Inbox receipt and second-device recovery remain manual checks.

## Cross-location rating history

- Changed “我评过的” from filtering the current nearby POI page to loading every restaurant snapshot associated with the signed-in user's ratings. Browser-only ratings retain the same snapshot in local storage until they are synced.
- Added an “全部 / 附近” range control. “全部” is the default; “附近” uses the same 2 km radius as discovery and the currently selected location. Search, category, and sort continue to apply inside the chosen range.
- Preserved map/list linkage for distant history entries: selecting a card moves and zooms the map to that restaurant. Public community ratings are fetched in bounded 50-POI batches.
- Extended the restaurant snapshot schema additively with business area, image URL, AMap rating, average cost, opening time, and source so historical cards remain useful outside the current AMap search page.
- Verified a cross-city restaurant through the local API account-state flow, then created and verified snapshot `dopabite-20260922T075212Z.sqlite3` before the production migration. Its off-host copy has matching SHA-256 `d7909f5df7cbaef0245b852aa0abdbe446e28859b5e42b9df7aadfd77e84ad6`.
- Deployed web and API release `20260922155323`. HTTPS, health/config endpoints, asset identity, Nginx, certificate SAN, database integrity and new columns passed; all 8 existing ratings retained their restaurant joins. Post-deploy storage is 26% of bytes and 10% of inodes.
