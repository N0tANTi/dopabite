# 2026-09-22 public repository and backend decision

- Created the public GitHub repository `https://github.com/N0tANTi/dopabite` and linked the production homepage.
- Replaced the README with a concise Chinese overview, live URL, feature list, setup instructions, data boundaries, and acknowledgements for `poi-marker` and Tastemap.
- Published a clean root history to keep the deleted local QA document and its machine-specific paths out of the public repository. The original local commits are retained in `archive/pre-public-history-20260922` and are intentionally not pushed.
- Verified that `.env.local`, credentials, dependencies, build output, and unused local assets are excluded from the public snapshot.
- Chose a progressive identity design: anonymous session for the first rating, Passkey for low-friction binding, future WeChat QR login, and email OTP only as a fallback.
- Chose a same-origin Node.js API with Better Auth and SQLite WAL for the first shared-rating backend. The decision reflects the current 2-core, 2 GB Tencent host and keeps persistent data outside release directories.
- Recorded the data model, API boundary, anti-abuse rules, privacy controls, backup requirements, local-rating import, and PostgreSQL migration triggers in `docs/architecture/shared-ratings.md`.

## Shared accounts and ratings deployment

- Implemented a same-origin Hono API with Better Auth anonymous sessions, Passkey enrollment/login, SQLite WAL migrations, public rating reads, per-user rating upserts, local-data import, and private saved-location sync.
- Replaced the inactive profile control with an account panel that explains data visibility, explicitly requests consent before uploading local favorites, supports Passkey binding, and clears the local personal cache on sign-out while keeping cloud data.
- Preserved offline behavior: rating writes remain in browser storage when the API is unavailable and can be merged later.
- Added input validation, same-origin write checks, bounded write limiting, secure response headers, non-public database storage, systemd hardening, API health checks, and atomic web/API release links.
- Fixed a React/Amap marker-root teardown race found during browser QA.
- Deployed web and API release `20260922110335` to `https://food.archein.site/`; enabled `dopabite-api.service` and daily verified backups.
- Verified two-user shared-rating behavior, private favorite isolation, rating update semantics, local-import preservation, HTTPS/API health, cross-origin rejection, certificate SAN, Nginx configuration, SQLite integrity, and post-deploy storage headroom.
- Copied the first production snapshot off-host with a matching checksum. Automated Tencent COS replication and a restore drill remain open.
