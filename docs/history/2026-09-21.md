# 2026-09-21 initial production deployment

- Completed the desktop map discovery flow with live AMap POIs, photos, AMap reference ratings, address search, adjustable result count, category-based numbered markers, marker/list synchronization, saved locations, map picking, and local DopaBite ratings.
- Removed the redundant manual-calibration card while preserving the map-pick action and location marker.
- Made the restaurant search bar sticky and added a bottom-right back-to-top control after the page is scrolled.
- Reduced the hero heading size and vertical spacing to increase the visible map area.
- Replaced marker emoji with Phosphor category icons.
- Deployed static release `20260921191049` to `/srv/dopabite/releases/20260921191049` and atomically activated `/srv/dopabite/current`.
- Added the `food.archein.site` Nginx virtual host, HTTP-to-HTTPS redirect, SPA fallback, immutable asset caching, and security headers.
- Issued a Let's Encrypt certificate for `food.archein.site`, expiring 2026-12-20, with automatic renewal enabled.
- Verified lint, production build, Nginx configuration, TLS identity, public HTTP/HTTPS behavior, live AMap loading, 40 POIs, and zero browser console errors.
- No Git remote was configured and the worktree contained mixed pre-existing changes, so this deployment was not committed or pushed. This remains a recovery risk tracked in `ROADMAP.md`.
